{
  "name": "netlib-auto-login",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build",
    "vercel-build": "npx playwright install-deps && npx playwright install chromium && node scripts/generate-vercel-config.mjs && npm run build"
  },
  "dependencies": {
    "next": "15.5.6",
    "playwright": "^1.40.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}